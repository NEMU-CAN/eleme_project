# 饿了么前端版项目框架说明

## 1. 项目定位
当前前端采用 `Vue 3 + TypeScript + Vite` 构建，是一个以课件为参考的点餐业务线前端雏形。页面重点不是后台联调，而是把“首页 - 商家列表 - 商家详情 - 结算 - 支付 - 订单 - 个人中心”这条主流程串完整。

## 2. 当前代码架构
项目结构是典型的分层式前端组织：

- `src/main.ts`：应用入口，负责挂载 Vue 和路由。
- `src/App.vue`：根容器，只承载路由视图。
- `src/router/index.ts`：统一管理页面路由，并在切换后更新标题。
- `src/composables/useHungryStore.ts`：核心状态层，集中管理商家、购物车、订单、用户、地址和结算草稿。
- `src/data/mock.ts`：静态 mock 数据，提供分类、商家、地址、默认用户和预置订单。
- `src/utils/format.ts`：金额、距离、时间、手机号脱敏等展示工具。
- `src/components/`：复用组件层，包含图标、头部、底部导航、分类九宫格、商家卡片、加减器。
- `src/views/`：页面层，分别承载首页、商家列表、商家详情、确认订单、在线支付、我的订单、我的、登录、注册和 404。
- `src/styles/global.css`：全局样式，统一页面尺寸、卡片、按钮、导航和各页面布局。

## 3. 已完成的功能
目前已经实现的内容比较完整，核心是前端交互闭环：

1. 首页展示地址、搜索入口、分类入口、推荐活动、会员模块和精选商家。
2. 商家列表支持综合、距离、销量三种排序。
3. 商家详情页支持菜单浏览、加减商品、购物车统计和去结算。
4. 确认订单页展示收货地址、商品明细、配送费和支付方式。
5. 在线支付页支持展开订单明细、切换支付方式、确认支付。
6. 我的订单页支持全部、未支付、已支付筛选，并可跳转支付。
7. 我的页面展示用户信息、统计数据、地址列表和快捷入口。
8. 登录、注册和 404 页面都已经补齐，前端流程不会断。

## 4. 数据流与状态流
当前项目没有接后端，所有页面都由本地 mock 数据和前端状态驱动。`useHungryStore.ts` 是整个项目的状态中枢，内部用 `reactive + computed` 组织数据，并通过 `localStorage` 做持久化，保证刷新后还能保留购物车、订单和用户信息。

状态里最关键的字段有：

- `activeMerchantId`：当前商家
- `cartByMerchantId`：按商家分组的购物车
- `orders`：订单列表
- `user`：当前用户
- `checkoutDraft`：结算草稿
- `addressId`：当前地址

这样做的好处是页面之间切换很顺，后面如果接真实接口，只需要把 `mock.ts` 和 store 的读写逻辑替换掉，大部分页面结构和交互都能保留。

## 5. 资源与页面风格
图片素材统一放在 `frontend/public/eleme/` 下，页面直接通过静态路径引用。整体视觉参考了课件里的网页版结构，但实现上做了更现代的 Vue 组件化处理，页面更规整，状态也更清晰。

## 6. 当前结论
现在的前端已经不是空框架，而是一个可跑通的点餐前端原型。它完成了课件要求里的主要页面和业务链路，也保留了后续接入后端时最容易替换的数据入口与状态层。
